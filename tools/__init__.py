def __init__(self, planner, researcher, summarizer, coach, timetable, memory=None, session=None):
    self.planner = planner
    self.researcher = researcher
    self.summarizer = summarizer
    self.coach = coach
    self.timetable = timetable
    self.memory = memory
    self.session = session
